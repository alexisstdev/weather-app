export const kelvinToCelsius = (temp) => Math.round(temp - 273.15);
export const fahrenheitToCelsius = (temp) => Math.round((temp - 32) / 1.8);
export const celsiusToFarenheit = (temp) => Math.round(temp * 1.8 + 32);
